#* Prints a paragraph about PubMed IDs not found in the MScanner database

notfound_pmids -- list of PubMed IDs (integer or string)

*#

#if $getVar("notfound_pmids", []) != []:

<h2>Invalid PubMed IDs</h2>

<p>Some of the PubMed IDs submitted to MScanner were not
found. Possible causes are:</p>

<ul>

<li>Some entries were "in-progress", and thus lack MeSH terms. If
their status has recently changed to "PubMed - Indexed for Medline" they will
be added to MScanner overnight.</li>

<li>Some entries were Medline UIs instead of PubMed IDs.  See
the <a href="http://www.ncbi.nlm.nih.gov/entrez/publisher/uidpmid.cgi">
Medline UI/PubMed ID matcher</a> to convert them to PubMed IDs</li>

<li>Some entries were invalid. For invalid PubMed IDs, clicking the links below
will lead to a blank page instead of a citation.</li>

</ul>

<ul>
#for pmid in $notfound_pmids:
<li><a href="http://www.ncbi.nlm.nih.gov/entrez/query.fcgi?cmd=Retrieve&db=pubmed&list_uids=$pmid">$pmid</a></li>
#end for
</ul>

#end if notfound_pmids