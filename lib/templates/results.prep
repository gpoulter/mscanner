<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">

<head>

  <title>Medline Scanner Results</title>

  <link rel="stylesheet" type="text/css" href="style.css"/>

  <style type="text/css">
  </style>
  
  <script type="text/javascript">
    current = null;
    function hide(id) {
    if (id == null) return;
    current = null;
    document.getElementById(id).style.display = "none";
    }
    function show(id) {
    hide(current);
    current = id;
    document.getElementById(id).style.display = "block";
    }
  </script>

</head>

<body>

<h1 id="heading">Results of MScanner Query</h1>

<div id="content">

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th>Pseudocount
        <button onclick="show('pseudocount_help')">?</button>
        </th>
        <td>{{pseudocount}}</td>
      </tr>
      <tr>
        <th>
          Result limit
          <button onclick="show('limit_help')">?</button>
        </th>
        <td>{{limit}}</td>
      </tr>
      <tr>
        <th>
          Number of results
        </th>
        <td>{{num_results}}</td>
      </tr>
      <tr>
        <th>
          Lowest scoring result
        </th>
        <td>{{'%.5f'%lowest_score}}</td>
      </tr>
      <tr>
        <th>
          Input PubMed IDs
          <button onclick="show('query_help')">?</button>
        </th>
        <td><a href="{{posfile}}">{{posfile}}</a></td>
      </tr>
      <tr>
        <th>
          MeSH Scores (CSV)
          <button onclick="show('csv_help')">?</button>
        </th>
        <td><a href="{{terms_csv}}">{{terms_csv}}</a></td>
      </tr>
      <tr>
        <th>
          MeSH scores (HTML)
          <button onclick="show('terms_help')">?</button>
        </th>
        <td><a href="{{terms_html}}">{{terms_html}}</a></td>
      </tr>
      <tr>
        <th>
          Result scores (TXT)
          <button onclick="show('results_help')">?</button>
        </th>
        <td><a href="{{res_txt}}">{{res_txt}}</a></td>
      </tr>
      <tr>
        <th>
          Input citations (HTML)
          <button onclick="show('input_citations_help')">?</button>
        </th>
        <td><a href="{{input_html}}">{{input_html}}</a></td>
      </tr>
      <tr>
        <th>
          Result citations (HTML)
          <button onclick="show('result_citations_help')">?</button>
        </th>
        <td><a href="{{result_html}}">{{result_html}}</a></td>
      </tr>
    </tbody>
  </table>

  <div class="help" id="pseudocount_help">
    Added to the number of occurrences of every term, to avoid divide
    by zero on terms which don't occur anywhere in a set of articles.
  </div>
  
  <div class="help" id="limit_help">
    The maximum number of results.
  </div>
  
  <div class="help" id="query_help">
    These are the PubMed IDs of the positive articles you provided.
  </div>
  
  <div class="help" id="csv_help">
    Spreadsheet table with the score of each MeSH term.  The score of
    a MeSH term is the natural logarithm of the ratio of the term
    frequency amongst the positive documents to the term frequency in
    Medline as a whole.  Each article has a number of MeSH terms
    associated with it, and the score for an article is the sum of the
    scores of its MeSH terms.  This file is more compact than the HTML
    below.
  </div>
  
  <div class="help" id="terms_help">
    HTML display of MeSH term scores. This file can be very large
    (2Mb).
  </div>
  
  <div class="help" id="results_help">
    Text file listing the result PubMed IDs and their scores, in
    decreasing order of score. More compact than the HTML version
    which also contains the citations.
  </div>
  
  <div class="help" id="input_citations_help">
    HTML file for the input citations used to train the classifier.
    Includes links to NCBI, and title and abstract.  This file is
    useful for seeing exactly what data you are using to train the
    classifier.
  </div>
  
  <div class="help" id="result_citations_help">
    HTML file for the result citations, including links to NCBI,
    titles and abstracts.  This file can be very large.
  </div>
  
</div>
  
</body>

</html>